/* Second half of the ./fitrig shim, see ./index.mjs. The demo page in the
   anatomy workspace reaches the rig directly for gripSides; keeping the path
   real here means a file copied in from there needs no edit either. */
export * from "../../rig.mjs";
